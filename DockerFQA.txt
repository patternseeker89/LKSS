1. Create Dockerfile
2. Build image:
	docker build -t lkss .
3. Run image:
    	docker run lkss
4. Run bash into container:
    	docker run -it lkss /bin/sh
5. Volume:
    	docker run -v $(pwd):/usr/src lkss
    	docker run -it -v $(pwd):/usr/src lkss /bin/sh
